import { Typography } from '@mui/material';
import React from 'react';

const Pay = () => {
    return (
        <>
            <Typography sx={{ fontSize: '40px', textAlign: 'center', fontWeight: 600 }}>
                Payment system coming soon.
            </Typography>
        </>
    );
};

export default Pay;